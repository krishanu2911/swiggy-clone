import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const GeineScreen = () => {
  return (
    <SafeAreaView className="flex-1">
      <Text>Geine</Text>
    </SafeAreaView>
  );
};

export default GeineScreen;
